// There's no way to make the linter happy for these long lines, so it's disabled.
/* eslint-disable max-len */
import { createPropertyNamesSlice } from '../table';

export const filterPropertyReferenceIdsSlice = createPropertyNamesSlice(
    'filter-property-reference-ids',
    'filterProperties'
);
